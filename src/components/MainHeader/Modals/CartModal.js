import Card from "../../UI/Card";
import Meal from "../../MealList/Meal";
const CartModal = () => {
  return (
    <Card>
      <Meal key={null} name={null} desc={null} price={null} />
    </Card>
  );
};
export default CartModal;
